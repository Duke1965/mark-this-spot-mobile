"use client";

export default function HomeClient() {
  return (
    <main style={{padding: 24, color: "white", fontFamily: "system-ui", background: "#0b1220", minHeight: "100vh"}}>
      <h1 style={{margin: 0}}>PINIT</h1>
      <p style={{opacity: 0.8}}>Boot check — if you can see this, the page and routing are fine.</p>
    </main>
  );
}
